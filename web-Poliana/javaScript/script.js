function verificar(){
    var data = new date()
    var ano = data.getUTFullYear()
    var fAno = document.getElementById(txtano)
    var res = document.getElementById(res)

}
if(fAno.value.lengt = 0 || Number(fAno.value)>ano){
    window.alert([ERRO] verifique os dados e tente novamente)
} else{
    var fsex = Document.getElementById(radsex)
    var idade = ano - Number (fAno.value)
    var = genero = 
    var emoji = document.createElement(emoji)
    emoji.fSexAttribute(id, emoji)
    if(fsex[0].checked){
        genero = Masculino
        if(idade >= 0 && idade < 10){
//criança//


        }
    }
    
}